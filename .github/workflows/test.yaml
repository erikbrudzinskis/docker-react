name: Tests
on:
  pull_request:
    branches: [master]
  push:
    branches: '!master'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test
        run: |
          npm install
          npm run test